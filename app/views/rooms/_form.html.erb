<%= form_for(room_form, as: :room,  url: room_form.action, method: room_form.method) do |f| %>
  <% if room_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room_form.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% room_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :capacity %><br>
    <%= f.number_field :capacity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
